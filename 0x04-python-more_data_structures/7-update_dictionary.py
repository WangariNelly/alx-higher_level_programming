#!/usr/bin/python3
def update_dictionary(a_dictionary, key, value):
    new_key = []
    for key in a_dictionary:
        if key == a_dictionary:
            a_dictionary[new_key] = a_dictionary.pop(key)
        if key ^  a_dictionary:
            a_dictionary.append(new_key)
        return new_key


        